# IMDB-database-parser

Program that stores the IMDB movie/actor/actress/director database into a binary tree and sorts it.

Allows the user search the tree with a file containing their search input.
